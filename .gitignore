# Secrets — never commit (contains a real PAT / credentials)
.env
.env.*
*.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtualenvs
.venv/
venv/

# Tooling caches
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Local-only demo / engagement material (not for publication)
DEMO.md
HUNTPLAN.md
scripts/mcp_auth_demo.py

# Run transcripts
runs/

# Range state (disk-backed poisonable corpus)
.cutout-range/

# uv
uv.lock

# OSS-hunt survey working dir (target lists, reports, findings — never publish)
study/
FINDING-*.md
poc-tunnel-client/
poc-codex/
FINDING-advisory-drafts/
